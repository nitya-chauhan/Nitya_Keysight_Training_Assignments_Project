import { FlightSortPipe } from './flight-sort.pipe';

describe('FlightSortPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightSortPipe();
    expect(pipe).toBeTruthy();
  });
});
